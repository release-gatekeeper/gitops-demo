name: validation
on:
  push:
    branches:
      - '*'
jobs:
  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        run: sleep 10
      - name: my-step
        run: echo "Hello World!"